# Screenshots for the main README

## Already in place

These are referenced from the main `README.md` — keep the names stable if you
replace one with a fresher capture.

| Filename | What it shows |
|---|---|
| `homepage.png` | The Doodle homepage (the hero image at the top of the README) |
| `campfire-line-art.png` | Camping with the Peppa Pig family, as the page prints |
| `campfire-coloured.png` | The same campfire scene coloured in |
| `characters.png` | The Your characters screen with its caricature portraits |
| `family-picnic.png` | All three saved characters at a picnic |
| `tent-line-art.png` | Putting the tent up, from the same camping request |

## Still wanted

Drop an image in with the exact name below, then open the main `README.md`,
find the matching block in its **Screenshots** section — each slot is two
lines, an image tag plus its caption — and delete the `<!--` and `-->`
comment markers around that block. The image appears on the repository
homepage the next time you push.

| Filename | What to capture |
|---|---|
| `studio.png` | The studio with a finished doodle ready to print |
| `make-a-change.png` | The *Make a change* box with a strip of earlier versions beneath a picture |
| `badge-sheet.png` | The badge sheet: repeated 58 mm circles on A4, cut lines included |
| `printed-and-coloured.jpg` | A printed page, coloured in — a phone photo is perfect |

PNG suits screen captures; JPG suits photographs. Keep each image roughly
1200–1600 px wide so it stays sharp on the README without being enormous.
Any extra screenshots you want to add can live here too — reference them from
the README as `docs/screenshots/<name>.png`.
